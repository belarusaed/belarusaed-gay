---
interface Props {
  date: Date;
  className: string;
}

const { date, className } = Astro.props;
---

<time class={className} datetime={date.toISOString()}>
  {
    date.toLocaleDateString('be-by', {
      year: 'numeric',
      month: 'long',
      day: 'numeric'
    })
  }
</time>
